# Dependencias de navegador

- `qrcode-generator` 1.4.4, licencia MIT. Se usa para generar QR localmente en el navegador; el token no se envía a un servicio externo.
- `jsPDF` 2.5.1, licencia MIT. Se usa para crear archivos PDF reales en el navegador, sin imprimir la página HTML.
