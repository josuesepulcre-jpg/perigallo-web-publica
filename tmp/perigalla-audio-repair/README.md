# Isolated V9 audio-repair workspace

This directory is reserved for non-publishable repair artifacts only: transcript runs, candidate clips, manifests and QA comparisons. It must never be used as a source of production audio until `reports/audio/VOICE_PIPELINE_BLOCKER.md` is resolved and a full lineage report permits the exact V9 pipeline.

The current workspace intentionally contains no regenerated audio.
